# Memory index

- [Verification commands](project_verification_commands.md) - typecheck via local node_modules/.bin/tsc (no npm), py_compile for Python; classify_matrix_view.py is duplicated in two backend dirs
